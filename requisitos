objetivo: contruir a tabela order.

desafio:
	ok - preencher sku da tabela product;
	ok - criar tabela img.
	ok - fazer /home/susan/programacao/projeto001/backend/cache/products.txt ser construido do banco de dados.
	ok - criar o cache de produtos a partir do banco.
	migrar do cache products.txt para bd_products.txt.
	definir a melhor modelagem da tabela order.
	fazer id da div virar product_id do banco.
	

create table order(
	order_id int not null auto_increment primary key,
	user_id int not null foreign key references user(user_id int),
	product_id int not null foreign key references product(product_id),
	quantity int not null,
	umb varchar(4) not null default=und,
);

